import { StyleSheet } from 'react-native';

export const employeeListingStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    emptyData: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    }
})